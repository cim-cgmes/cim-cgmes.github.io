# 61970-600-2_ShortCircuit-AP-Con-Simple-SHACL

## sc:ACLineSegment

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:ACLineSegment

**Nested Properties:**

### sc:ACLineSegment.b0ch-cardinality

**Path:** `cim:ACLineSegment.b0ch`  
**Name:** ACLineSegment.b0ch-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ACLineSegment.b0ch-datatype

**Path:** `cim:ACLineSegment.b0ch`  
**Name:** ACLineSegment.b0ch-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ACLineSegment.g0ch-datatype

**Path:** `cim:ACLineSegment.g0ch`  
**Name:** ACLineSegment.g0ch-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ACLineSegment.g0ch-cardinality

**Path:** `cim:ACLineSegment.g0ch`  
**Name:** ACLineSegment.g0ch-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ACLineSegment.r0-datatype

**Path:** `cim:ACLineSegment.r0`  
**Name:** ACLineSegment.r0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ACLineSegment.r0-cardinality

**Path:** `cim:ACLineSegment.r0`  
**Name:** ACLineSegment.r0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ACLineSegment.shortCircuitEndTemperature-cardinality

**Path:** `cim:ACLineSegment.shortCircuitEndTemperature`  
**Name:** ACLineSegment.shortCircuitEndTemperature-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ACLineSegment.shortCircuitEndTemperature-datatype

**Path:** `cim:ACLineSegment.shortCircuitEndTemperature`  
**Name:** ACLineSegment.shortCircuitEndTemperature-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ACLineSegment.x0-cardinality

**Path:** `cim:ACLineSegment.x0`  
**Name:** ACLineSegment.x0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ACLineSegment.x0-datatype

**Path:** `cim:ACLineSegment.x0`  
**Name:** ACLineSegment.x0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

## sc:AsynchronousMachine

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:AsynchronousMachine

**Nested Properties:**

### sc:AsynchronousMachine.converterFedDrive-cardinality

**Path:** `cim:AsynchronousMachine.converterFedDrive`  
**Name:** AsynchronousMachine.converterFedDrive-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:AsynchronousMachine.converterFedDrive-datatype

**Path:** `cim:AsynchronousMachine.converterFedDrive`  
**Name:** AsynchronousMachine.converterFedDrive-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:boolean` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:AsynchronousMachine.efficiency-cardinality

**Path:** `cim:AsynchronousMachine.efficiency`  
**Name:** AsynchronousMachine.efficiency-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:AsynchronousMachine.efficiency-datatype

**Path:** `cim:AsynchronousMachine.efficiency`  
**Name:** AsynchronousMachine.efficiency-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:AsynchronousMachine.iaIrRatio-datatype

**Path:** `cim:AsynchronousMachine.iaIrRatio`  
**Name:** AsynchronousMachine.iaIrRatio-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:AsynchronousMachine.iaIrRatio-cardinality

**Path:** `cim:AsynchronousMachine.iaIrRatio`  
**Name:** AsynchronousMachine.iaIrRatio-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:AsynchronousMachine.polePairNumber-cardinality

**Path:** `cim:AsynchronousMachine.polePairNumber`  
**Name:** AsynchronousMachine.polePairNumber-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:AsynchronousMachine.polePairNumber-datatype

**Path:** `cim:AsynchronousMachine.polePairNumber`  
**Name:** AsynchronousMachine.polePairNumber-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:integer` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:AsynchronousMachine.ratedMechanicalPower-datatype

**Path:** `cim:AsynchronousMachine.ratedMechanicalPower`  
**Name:** AsynchronousMachine.ratedMechanicalPower-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:AsynchronousMachine.ratedMechanicalPower-cardinality

**Path:** `cim:AsynchronousMachine.ratedMechanicalPower`  
**Name:** AsynchronousMachine.ratedMechanicalPower-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:AsynchronousMachine.reversible-datatype

**Path:** `cim:AsynchronousMachine.reversible`  
**Name:** AsynchronousMachine.reversible-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:boolean` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:AsynchronousMachine.reversible-cardinality

**Path:** `cim:AsynchronousMachine.reversible`  
**Name:** AsynchronousMachine.reversible-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:AsynchronousMachine.rxLockedRotorRatio-cardinality

**Path:** `cim:AsynchronousMachine.rxLockedRotorRatio`  
**Name:** AsynchronousMachine.rxLockedRotorRatio-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:AsynchronousMachine.rxLockedRotorRatio-datatype

**Path:** `cim:AsynchronousMachine.rxLockedRotorRatio`  
**Name:** AsynchronousMachine.rxLockedRotorRatio-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

## sc:BusbarSection

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:BusbarSection

**Nested Properties:**

### sc:BusbarSection.ipMax-datatype

**Path:** `cim:BusbarSection.ipMax`  
**Name:** BusbarSection.ipMax-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:BusbarSection.ipMax-cardinality

**Path:** `cim:BusbarSection.ipMax`  
**Name:** BusbarSection.ipMax-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

## sc:EnergySource

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:EnergySource

**Nested Properties:**

### sc:EnergySource.r-datatype

**Path:** `cim:EnergySource.r`  
**Name:** EnergySource.r-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EnergySource.r-cardinality

**Path:** `cim:EnergySource.r`  
**Name:** EnergySource.r-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EnergySource.r0-datatype

**Path:** `cim:EnergySource.r0`  
**Name:** EnergySource.r0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EnergySource.r0-cardinality

**Path:** `cim:EnergySource.r0`  
**Name:** EnergySource.r0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EnergySource.rn-cardinality

**Path:** `cim:EnergySource.rn`  
**Name:** EnergySource.rn-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EnergySource.rn-datatype

**Path:** `cim:EnergySource.rn`  
**Name:** EnergySource.rn-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EnergySource.x-datatype

**Path:** `cim:EnergySource.x`  
**Name:** EnergySource.x-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EnergySource.x-cardinality

**Path:** `cim:EnergySource.x`  
**Name:** EnergySource.x-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EnergySource.x0-datatype

**Path:** `cim:EnergySource.x0`  
**Name:** EnergySource.x0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EnergySource.x0-cardinality

**Path:** `cim:EnergySource.x0`  
**Name:** EnergySource.x0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EnergySource.xn-cardinality

**Path:** `cim:EnergySource.xn`  
**Name:** EnergySource.xn-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EnergySource.xn-datatype

**Path:** `cim:EnergySource.xn`  
**Name:** EnergySource.xn-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

## sc:EquivalentBranch

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:EquivalentBranch

**Nested Properties:**

### sc:EquivalentBranch.negativeR12-datatype

**Path:** `cim:EquivalentBranch.negativeR12`  
**Name:** EquivalentBranch.negativeR12-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.negativeR12-cardinality

**Path:** `cim:EquivalentBranch.negativeR12`  
**Name:** EquivalentBranch.negativeR12-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.negativeR21-datatype

**Path:** `cim:EquivalentBranch.negativeR21`  
**Name:** EquivalentBranch.negativeR21-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.negativeR21-cardinality

**Path:** `cim:EquivalentBranch.negativeR21`  
**Name:** EquivalentBranch.negativeR21-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.negativeX12-datatype

**Path:** `cim:EquivalentBranch.negativeX12`  
**Name:** EquivalentBranch.negativeX12-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.negativeX12-cardinality

**Path:** `cim:EquivalentBranch.negativeX12`  
**Name:** EquivalentBranch.negativeX12-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.negativeX21-datatype

**Path:** `cim:EquivalentBranch.negativeX21`  
**Name:** EquivalentBranch.negativeX21-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.negativeX21-cardinality

**Path:** `cim:EquivalentBranch.negativeX21`  
**Name:** EquivalentBranch.negativeX21-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.positiveR12-cardinality

**Path:** `cim:EquivalentBranch.positiveR12`  
**Name:** EquivalentBranch.positiveR12-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.positiveR12-datatype

**Path:** `cim:EquivalentBranch.positiveR12`  
**Name:** EquivalentBranch.positiveR12-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.positiveR21-cardinality

**Path:** `cim:EquivalentBranch.positiveR21`  
**Name:** EquivalentBranch.positiveR21-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.positiveR21-datatype

**Path:** `cim:EquivalentBranch.positiveR21`  
**Name:** EquivalentBranch.positiveR21-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.positiveX12-cardinality

**Path:** `cim:EquivalentBranch.positiveX12`  
**Name:** EquivalentBranch.positiveX12-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.positiveX12-datatype

**Path:** `cim:EquivalentBranch.positiveX12`  
**Name:** EquivalentBranch.positiveX12-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.positiveX21-datatype

**Path:** `cim:EquivalentBranch.positiveX21`  
**Name:** EquivalentBranch.positiveX21-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.positiveX21-cardinality

**Path:** `cim:EquivalentBranch.positiveX21`  
**Name:** EquivalentBranch.positiveX21-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.zeroR12-datatype

**Path:** `cim:EquivalentBranch.zeroR12`  
**Name:** EquivalentBranch.zeroR12-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.zeroR12-cardinality

**Path:** `cim:EquivalentBranch.zeroR12`  
**Name:** EquivalentBranch.zeroR12-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.zeroR21-datatype

**Path:** `cim:EquivalentBranch.zeroR21`  
**Name:** EquivalentBranch.zeroR21-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.zeroR21-cardinality

**Path:** `cim:EquivalentBranch.zeroR21`  
**Name:** EquivalentBranch.zeroR21-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.zeroX12-cardinality

**Path:** `cim:EquivalentBranch.zeroX12`  
**Name:** EquivalentBranch.zeroX12-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentBranch.zeroX12-datatype

**Path:** `cim:EquivalentBranch.zeroX12`  
**Name:** EquivalentBranch.zeroX12-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.zeroX21-datatype

**Path:** `cim:EquivalentBranch.zeroX21`  
**Name:** EquivalentBranch.zeroX21-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentBranch.zeroX21-cardinality

**Path:** `cim:EquivalentBranch.zeroX21`  
**Name:** EquivalentBranch.zeroX21-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

## sc:EquivalentInjection

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:EquivalentInjection

**Nested Properties:**

### sc:EquivalentInjection.r-datatype

**Path:** `cim:EquivalentInjection.r`  
**Name:** EquivalentInjection.r-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentInjection.r-cardinality

**Path:** `cim:EquivalentInjection.r`  
**Name:** EquivalentInjection.r-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentInjection.r0-cardinality

**Path:** `cim:EquivalentInjection.r0`  
**Name:** EquivalentInjection.r0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentInjection.r0-datatype

**Path:** `cim:EquivalentInjection.r0`  
**Name:** EquivalentInjection.r0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentInjection.r2-cardinality

**Path:** `cim:EquivalentInjection.r2`  
**Name:** EquivalentInjection.r2-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentInjection.r2-datatype

**Path:** `cim:EquivalentInjection.r2`  
**Name:** EquivalentInjection.r2-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentInjection.x-datatype

**Path:** `cim:EquivalentInjection.x`  
**Name:** EquivalentInjection.x-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentInjection.x-cardinality

**Path:** `cim:EquivalentInjection.x`  
**Name:** EquivalentInjection.x-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentInjection.x0-datatype

**Path:** `cim:EquivalentInjection.x0`  
**Name:** EquivalentInjection.x0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentInjection.x0-cardinality

**Path:** `cim:EquivalentInjection.x0`  
**Name:** EquivalentInjection.x0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EquivalentInjection.x2-datatype

**Path:** `cim:EquivalentInjection.x2`  
**Name:** EquivalentInjection.x2-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EquivalentInjection.x2-cardinality

**Path:** `cim:EquivalentInjection.x2`  
**Name:** EquivalentInjection.x2-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

## sc:ExternalNetworkInjection

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:ExternalNetworkInjection

**Nested Properties:**

### sc:ExternalNetworkInjection.ikSecond-cardinality

**Path:** `cim:ExternalNetworkInjection.ikSecond`  
**Name:** ExternalNetworkInjection.ikSecond-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ExternalNetworkInjection.ikSecond-datatype

**Path:** `cim:ExternalNetworkInjection.ikSecond`  
**Name:** ExternalNetworkInjection.ikSecond-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:boolean` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ExternalNetworkInjection.maxInitialSymShCCurrent-datatype

**Path:** `cim:ExternalNetworkInjection.maxInitialSymShCCurrent`  
**Name:** ExternalNetworkInjection.maxInitialSymShCCurrent-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ExternalNetworkInjection.maxInitialSymShCCurrent-cardinality

**Path:** `cim:ExternalNetworkInjection.maxInitialSymShCCurrent`  
**Name:** ExternalNetworkInjection.maxInitialSymShCCurrent-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ExternalNetworkInjection.maxR0ToX0Ratio-cardinality

**Path:** `cim:ExternalNetworkInjection.maxR0ToX0Ratio`  
**Name:** ExternalNetworkInjection.maxR0ToX0Ratio-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ExternalNetworkInjection.maxR0ToX0Ratio-datatype

**Path:** `cim:ExternalNetworkInjection.maxR0ToX0Ratio`  
**Name:** ExternalNetworkInjection.maxR0ToX0Ratio-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ExternalNetworkInjection.maxR1ToX1Ratio-datatype

**Path:** `cim:ExternalNetworkInjection.maxR1ToX1Ratio`  
**Name:** ExternalNetworkInjection.maxR1ToX1Ratio-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ExternalNetworkInjection.maxR1ToX1Ratio-cardinality

**Path:** `cim:ExternalNetworkInjection.maxR1ToX1Ratio`  
**Name:** ExternalNetworkInjection.maxR1ToX1Ratio-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ExternalNetworkInjection.maxZ0ToZ1Ratio-cardinality

**Path:** `cim:ExternalNetworkInjection.maxZ0ToZ1Ratio`  
**Name:** ExternalNetworkInjection.maxZ0ToZ1Ratio-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ExternalNetworkInjection.maxZ0ToZ1Ratio-datatype

**Path:** `cim:ExternalNetworkInjection.maxZ0ToZ1Ratio`  
**Name:** ExternalNetworkInjection.maxZ0ToZ1Ratio-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ExternalNetworkInjection.minInitialSymShCCurrent-datatype

**Path:** `cim:ExternalNetworkInjection.minInitialSymShCCurrent`  
**Name:** ExternalNetworkInjection.minInitialSymShCCurrent-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ExternalNetworkInjection.minInitialSymShCCurrent-cardinality

**Path:** `cim:ExternalNetworkInjection.minInitialSymShCCurrent`  
**Name:** ExternalNetworkInjection.minInitialSymShCCurrent-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ExternalNetworkInjection.minR0ToX0Ratio-datatype

**Path:** `cim:ExternalNetworkInjection.minR0ToX0Ratio`  
**Name:** ExternalNetworkInjection.minR0ToX0Ratio-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ExternalNetworkInjection.minR0ToX0Ratio-cardinality

**Path:** `cim:ExternalNetworkInjection.minR0ToX0Ratio`  
**Name:** ExternalNetworkInjection.minR0ToX0Ratio-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ExternalNetworkInjection.minR1ToX1Ratio-cardinality

**Path:** `cim:ExternalNetworkInjection.minR1ToX1Ratio`  
**Name:** ExternalNetworkInjection.minR1ToX1Ratio-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ExternalNetworkInjection.minR1ToX1Ratio-datatype

**Path:** `cim:ExternalNetworkInjection.minR1ToX1Ratio`  
**Name:** ExternalNetworkInjection.minR1ToX1Ratio-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ExternalNetworkInjection.minZ0ToZ1Ratio-datatype

**Path:** `cim:ExternalNetworkInjection.minZ0ToZ1Ratio`  
**Name:** ExternalNetworkInjection.minZ0ToZ1Ratio-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ExternalNetworkInjection.minZ0ToZ1Ratio-cardinality

**Path:** `cim:ExternalNetworkInjection.minZ0ToZ1Ratio`  
**Name:** ExternalNetworkInjection.minZ0ToZ1Ratio-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:ExternalNetworkInjection.voltageFactor-datatype

**Path:** `cim:ExternalNetworkInjection.voltageFactor`  
**Name:** ExternalNetworkInjection.voltageFactor-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:ExternalNetworkInjection.voltageFactor-cardinality

**Path:** `cim:ExternalNetworkInjection.voltageFactor`  
**Name:** ExternalNetworkInjection.voltageFactor-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

## sc:GroundingImpedance

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:GroundingImpedance

**Nested Properties:**

### sc:EarthFaultCompensator.r-datatype

**Path:** `cim:EarthFaultCompensator.r`  
**Name:** EarthFaultCompensator.r-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:EarthFaultCompensator.r-cardinality

**Path:** `cim:EarthFaultCompensator.r`  
**Name:** EarthFaultCompensator.r-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:GroundingImpedance.x-datatype

**Path:** `cim:GroundingImpedance.x`  
**Name:** GroundingImpedance.x-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:GroundingImpedance.x-cardinality

**Path:** `cim:GroundingImpedance.x`  
**Name:** GroundingImpedance.x-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

## sc:LinearShuntCompensator

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:LinearShuntCompensator

**Nested Properties:**

### sc:LinearShuntCompensator.b0PerSection-datatype

**Path:** `cim:LinearShuntCompensator.b0PerSection`  
**Name:** LinearShuntCompensator.b0PerSection-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:LinearShuntCompensator.b0PerSection-cardinality

**Path:** `cim:LinearShuntCompensator.b0PerSection`  
**Name:** LinearShuntCompensator.b0PerSection-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:LinearShuntCompensator.g0PerSection-datatype

**Path:** `cim:LinearShuntCompensator.g0PerSection`  
**Name:** LinearShuntCompensator.g0PerSection-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:LinearShuntCompensator.g0PerSection-cardinality

**Path:** `cim:LinearShuntCompensator.g0PerSection`  
**Name:** LinearShuntCompensator.g0PerSection-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

## sc:MutualCoupling

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:MutualCoupling

**Nested Properties:**

### io:IdentifiedObject.mRID-cardinality

**Path:** `cim:IdentifiedObject.mRID`  
**Name:** IdentifiedObject.mRID-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### io:IdentifiedObject.mRID-datatype

**Path:** `cim:IdentifiedObject.mRID`  
**Name:** IdentifiedObject.mRID-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:string` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:MutualCoupling.First_Terminal-cardinality

**Path:** `cim:MutualCoupling.First_Terminal`  
**Name:** MutualCoupling.First_Terminal-cardinality  
This constraint validates the cardinality of the association at the used direction.

**Severity:** sh:Violation

**Messages:**
- "Missing required association."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:MutualCoupling.Second_Terminal-cardinality

**Path:** `cim:MutualCoupling.Second_Terminal`  
**Name:** MutualCoupling.Second_Terminal-cardinality  
This constraint validates the cardinality of the association at the used direction.

**Severity:** sh:Violation

**Messages:**
- "Missing required association."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:MutualCoupling.b0ch-datatype

**Path:** `cim:MutualCoupling.b0ch`  
**Name:** MutualCoupling.b0ch-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:MutualCoupling.b0ch-cardinality

**Path:** `cim:MutualCoupling.b0ch`  
**Name:** MutualCoupling.b0ch-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:MutualCoupling.distance11-cardinality

**Path:** `cim:MutualCoupling.distance11`  
**Name:** MutualCoupling.distance11-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:MutualCoupling.distance11-datatype

**Path:** `cim:MutualCoupling.distance11`  
**Name:** MutualCoupling.distance11-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:MutualCoupling.distance12-datatype

**Path:** `cim:MutualCoupling.distance12`  
**Name:** MutualCoupling.distance12-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:MutualCoupling.distance12-cardinality

**Path:** `cim:MutualCoupling.distance12`  
**Name:** MutualCoupling.distance12-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:MutualCoupling.distance21-datatype

**Path:** `cim:MutualCoupling.distance21`  
**Name:** MutualCoupling.distance21-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:MutualCoupling.distance21-cardinality

**Path:** `cim:MutualCoupling.distance21`  
**Name:** MutualCoupling.distance21-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:MutualCoupling.distance22-datatype

**Path:** `cim:MutualCoupling.distance22`  
**Name:** MutualCoupling.distance22-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:MutualCoupling.distance22-cardinality

**Path:** `cim:MutualCoupling.distance22`  
**Name:** MutualCoupling.distance22-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:MutualCoupling.g0ch-datatype

**Path:** `cim:MutualCoupling.g0ch`  
**Name:** MutualCoupling.g0ch-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:MutualCoupling.g0ch-cardinality

**Path:** `cim:MutualCoupling.g0ch`  
**Name:** MutualCoupling.g0ch-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:MutualCoupling.r0-datatype

**Path:** `cim:MutualCoupling.r0`  
**Name:** MutualCoupling.r0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:MutualCoupling.r0-cardinality

**Path:** `cim:MutualCoupling.r0`  
**Name:** MutualCoupling.r0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:MutualCoupling.x0-cardinality

**Path:** `cim:MutualCoupling.x0`  
**Name:** MutualCoupling.x0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:MutualCoupling.x0-datatype

**Path:** `cim:MutualCoupling.x0`  
**Name:** MutualCoupling.x0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

## sc:NonlinearShuntCompensatorPoint

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:NonlinearShuntCompensatorPoint

**Nested Properties:**

### sc:NonlinearShuntCompensatorPoint.b0-datatype

**Path:** `cim:NonlinearShuntCompensatorPoint.b0`  
**Name:** NonlinearShuntCompensatorPoint.b0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:NonlinearShuntCompensatorPoint.b0-cardinality

**Path:** `cim:NonlinearShuntCompensatorPoint.b0`  
**Name:** NonlinearShuntCompensatorPoint.b0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:NonlinearShuntCompensatorPoint.g0-datatype

**Path:** `cim:NonlinearShuntCompensatorPoint.g0`  
**Name:** NonlinearShuntCompensatorPoint.g0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:NonlinearShuntCompensatorPoint.g0-cardinality

**Path:** `cim:NonlinearShuntCompensatorPoint.g0`  
**Name:** NonlinearShuntCompensatorPoint.g0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

## sc:PetersenCoil

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:PetersenCoil

**Nested Properties:**

### sc:EarthFaultCompensator.r-cardinality

**Path:** `cim:EarthFaultCompensator.r`  
**Name:** EarthFaultCompensator.r-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:EarthFaultCompensator.r-datatype

**Path:** `cim:EarthFaultCompensator.r`  
**Name:** EarthFaultCompensator.r-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PetersenCoil.mode-cardinality

**Path:** `cim:PetersenCoil.mode`  
**Name:** PetersenCoil.mode-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PetersenCoil.mode-datatype

**Path:** `cim:PetersenCoil.mode`  
**Name:** PetersenCoil.mode-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not IRI (Internationalized Resource Identifier) or it is enumerated value not part of the profile."

**Constraints:**

- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:IRI` 
- **sh:InConstraintComponent** (Severity: sh:Violation)
  - Values: `[cim:PetersenCoilModeKind.fixed cim:PetersenCoilModeKind.manual cim:PetersenCoilModeKind.automaticPositioning]` 

### sc:PetersenCoil.nominalU-datatype

**Path:** `cim:PetersenCoil.nominalU`  
**Name:** PetersenCoil.nominalU-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PetersenCoil.nominalU-cardinality

**Path:** `cim:PetersenCoil.nominalU`  
**Name:** PetersenCoil.nominalU-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PetersenCoil.offsetCurrent-cardinality

**Path:** `cim:PetersenCoil.offsetCurrent`  
**Name:** PetersenCoil.offsetCurrent-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PetersenCoil.offsetCurrent-datatype

**Path:** `cim:PetersenCoil.offsetCurrent`  
**Name:** PetersenCoil.offsetCurrent-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PetersenCoil.positionCurrent-cardinality

**Path:** `cim:PetersenCoil.positionCurrent`  
**Name:** PetersenCoil.positionCurrent-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PetersenCoil.positionCurrent-datatype

**Path:** `cim:PetersenCoil.positionCurrent`  
**Name:** PetersenCoil.positionCurrent-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PetersenCoil.xGroundMax-datatype

**Path:** `cim:PetersenCoil.xGroundMax`  
**Name:** PetersenCoil.xGroundMax-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PetersenCoil.xGroundMax-cardinality

**Path:** `cim:PetersenCoil.xGroundMax`  
**Name:** PetersenCoil.xGroundMax-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PetersenCoil.xGroundMin-cardinality

**Path:** `cim:PetersenCoil.xGroundMin`  
**Name:** PetersenCoil.xGroundMin-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PetersenCoil.xGroundMin-datatype

**Path:** `cim:PetersenCoil.xGroundMin`  
**Name:** PetersenCoil.xGroundMin-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PetersenCoil.xGroundNominal-datatype

**Path:** `cim:PetersenCoil.xGroundNominal`  
**Name:** PetersenCoil.xGroundNominal-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PetersenCoil.xGroundNominal-cardinality

**Path:** `cim:PetersenCoil.xGroundNominal`  
**Name:** PetersenCoil.xGroundNominal-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

## sc:PowerTransformer

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:PowerTransformer

**Nested Properties:**

### sc:PowerTransformer.beforeShCircuitHighestOperatingCurrent-cardinality

**Path:** `cim:PowerTransformer.beforeShCircuitHighestOperatingCurrent`  
**Name:** PowerTransformer.beforeShCircuitHighestOperatingCurrent-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformer.beforeShCircuitHighestOperatingCurrent-datatype

**Path:** `cim:PowerTransformer.beforeShCircuitHighestOperatingCurrent`  
**Name:** PowerTransformer.beforeShCircuitHighestOperatingCurrent-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PowerTransformer.beforeShCircuitHighestOperatingVoltage-cardinality

**Path:** `cim:PowerTransformer.beforeShCircuitHighestOperatingVoltage`  
**Name:** PowerTransformer.beforeShCircuitHighestOperatingVoltage-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformer.beforeShCircuitHighestOperatingVoltage-datatype

**Path:** `cim:PowerTransformer.beforeShCircuitHighestOperatingVoltage`  
**Name:** PowerTransformer.beforeShCircuitHighestOperatingVoltage-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PowerTransformer.beforeShortCircuitAnglePf-datatype

**Path:** `cim:PowerTransformer.beforeShortCircuitAnglePf`  
**Name:** PowerTransformer.beforeShortCircuitAnglePf-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PowerTransformer.beforeShortCircuitAnglePf-cardinality

**Path:** `cim:PowerTransformer.beforeShortCircuitAnglePf`  
**Name:** PowerTransformer.beforeShortCircuitAnglePf-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformer.highSideMinOperatingU-cardinality

**Path:** `cim:PowerTransformer.highSideMinOperatingU`  
**Name:** PowerTransformer.highSideMinOperatingU-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformer.highSideMinOperatingU-datatype

**Path:** `cim:PowerTransformer.highSideMinOperatingU`  
**Name:** PowerTransformer.highSideMinOperatingU-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PowerTransformer.isPartOfGeneratorUnit-datatype

**Path:** `cim:PowerTransformer.isPartOfGeneratorUnit`  
**Name:** PowerTransformer.isPartOfGeneratorUnit-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:boolean` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PowerTransformer.isPartOfGeneratorUnit-cardinality

**Path:** `cim:PowerTransformer.isPartOfGeneratorUnit`  
**Name:** PowerTransformer.isPartOfGeneratorUnit-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformer.operationalValuesConsidered-cardinality

**Path:** `cim:PowerTransformer.operationalValuesConsidered`  
**Name:** PowerTransformer.operationalValuesConsidered-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformer.operationalValuesConsidered-datatype

**Path:** `cim:PowerTransformer.operationalValuesConsidered`  
**Name:** PowerTransformer.operationalValuesConsidered-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:boolean` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

## sc:PowerTransformerEnd

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:PowerTransformerEnd

**Nested Properties:**

### sc:PowerTransformerEnd.b0-cardinality

**Path:** `cim:PowerTransformerEnd.b0`  
**Name:** PowerTransformerEnd.b0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformerEnd.b0-datatype

**Path:** `cim:PowerTransformerEnd.b0`  
**Name:** PowerTransformerEnd.b0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PowerTransformerEnd.g0-cardinality

**Path:** `cim:PowerTransformerEnd.g0`  
**Name:** PowerTransformerEnd.g0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformerEnd.g0-datatype

**Path:** `cim:PowerTransformerEnd.g0`  
**Name:** PowerTransformerEnd.g0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PowerTransformerEnd.phaseAngleClock-datatype

**Path:** `cim:PowerTransformerEnd.phaseAngleClock`  
**Name:** PowerTransformerEnd.phaseAngleClock-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:integer` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PowerTransformerEnd.phaseAngleClock-cardinality

**Path:** `cim:PowerTransformerEnd.phaseAngleClock`  
**Name:** PowerTransformerEnd.phaseAngleClock-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformerEnd.r0-cardinality

**Path:** `cim:PowerTransformerEnd.r0`  
**Name:** PowerTransformerEnd.r0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformerEnd.r0-datatype

**Path:** `cim:PowerTransformerEnd.r0`  
**Name:** PowerTransformerEnd.r0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:PowerTransformerEnd.x0-cardinality

**Path:** `cim:PowerTransformerEnd.x0`  
**Name:** PowerTransformerEnd.x0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:PowerTransformerEnd.x0-datatype

**Path:** `cim:PowerTransformerEnd.x0`  
**Name:** PowerTransformerEnd.x0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:TransformerEnd.grounded-datatype

**Path:** `cim:TransformerEnd.grounded`  
**Name:** TransformerEnd.grounded-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:boolean` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:TransformerEnd.grounded-cardinality

**Path:** `cim:TransformerEnd.grounded`  
**Name:** TransformerEnd.grounded-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:TransformerEnd.rground-cardinality

**Path:** `cim:TransformerEnd.rground`  
**Name:** TransformerEnd.rground-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:TransformerEnd.rground-datatype

**Path:** `cim:TransformerEnd.rground`  
**Name:** TransformerEnd.rground-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:TransformerEnd.xground-cardinality

**Path:** `cim:TransformerEnd.xground`  
**Name:** TransformerEnd.xground-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:TransformerEnd.xground-datatype

**Path:** `cim:TransformerEnd.xground`  
**Name:** TransformerEnd.xground-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

## sc:SeriesCompensator

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:SeriesCompensator

**Nested Properties:**

### sc:SeriesCompensator.r0-cardinality

**Path:** `cim:SeriesCompensator.r0`  
**Name:** SeriesCompensator.r0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SeriesCompensator.r0-datatype

**Path:** `cim:SeriesCompensator.r0`  
**Name:** SeriesCompensator.r0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SeriesCompensator.varistorPresent-datatype

**Path:** `cim:SeriesCompensator.varistorPresent`  
**Name:** SeriesCompensator.varistorPresent-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:boolean` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SeriesCompensator.varistorPresent-cardinality

**Path:** `cim:SeriesCompensator.varistorPresent`  
**Name:** SeriesCompensator.varistorPresent-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SeriesCompensator.varistorRatedCurrent-cardinality

**Path:** `cim:SeriesCompensator.varistorRatedCurrent`  
**Name:** SeriesCompensator.varistorRatedCurrent-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SeriesCompensator.varistorRatedCurrent-datatype

**Path:** `cim:SeriesCompensator.varistorRatedCurrent`  
**Name:** SeriesCompensator.varistorRatedCurrent-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SeriesCompensator.varistorVoltageThreshold-cardinality

**Path:** `cim:SeriesCompensator.varistorVoltageThreshold`  
**Name:** SeriesCompensator.varistorVoltageThreshold-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SeriesCompensator.varistorVoltageThreshold-datatype

**Path:** `cim:SeriesCompensator.varistorVoltageThreshold`  
**Name:** SeriesCompensator.varistorVoltageThreshold-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SeriesCompensator.x0-cardinality

**Path:** `cim:SeriesCompensator.x0`  
**Name:** SeriesCompensator.x0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SeriesCompensator.x0-datatype

**Path:** `cim:SeriesCompensator.x0`  
**Name:** SeriesCompensator.x0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

## sc:SynchronousMachine

**Severity:** sh:Violation

**Targets:**
- targetClass: cim:SynchronousMachine

**Nested Properties:**

### sc:SynchronousMachine.earthing-cardinality

**Path:** `cim:SynchronousMachine.earthing`  
**Name:** SynchronousMachine.earthing-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.earthing-datatype

**Path:** `cim:SynchronousMachine.earthing`  
**Name:** SynchronousMachine.earthing-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:boolean` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.earthingStarPointR-datatype

**Path:** `cim:SynchronousMachine.earthingStarPointR`  
**Name:** SynchronousMachine.earthingStarPointR-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.earthingStarPointR-cardinality

**Path:** `cim:SynchronousMachine.earthingStarPointR`  
**Name:** SynchronousMachine.earthingStarPointR-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.earthingStarPointX-cardinality

**Path:** `cim:SynchronousMachine.earthingStarPointX`  
**Name:** SynchronousMachine.earthingStarPointX-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.earthingStarPointX-datatype

**Path:** `cim:SynchronousMachine.earthingStarPointX`  
**Name:** SynchronousMachine.earthingStarPointX-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.ikk-cardinality

**Path:** `cim:SynchronousMachine.ikk`  
**Name:** SynchronousMachine.ikk-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.ikk-datatype

**Path:** `cim:SynchronousMachine.ikk`  
**Name:** SynchronousMachine.ikk-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.mu-cardinality

**Path:** `cim:SynchronousMachine.mu`  
**Name:** SynchronousMachine.mu-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.mu-datatype

**Path:** `cim:SynchronousMachine.mu`  
**Name:** SynchronousMachine.mu-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.r-cardinality

**Path:** `cim:SynchronousMachine.r`  
**Name:** SynchronousMachine.r-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.r-datatype

**Path:** `cim:SynchronousMachine.r`  
**Name:** SynchronousMachine.r-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.r0-datatype

**Path:** `cim:SynchronousMachine.r0`  
**Name:** SynchronousMachine.r0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.r0-cardinality

**Path:** `cim:SynchronousMachine.r0`  
**Name:** SynchronousMachine.r0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.r2-cardinality

**Path:** `cim:SynchronousMachine.r2`  
**Name:** SynchronousMachine.r2-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.r2-datatype

**Path:** `cim:SynchronousMachine.r2`  
**Name:** SynchronousMachine.r2-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.satDirectSubtransX-cardinality

**Path:** `cim:SynchronousMachine.satDirectSubtransX`  
**Name:** SynchronousMachine.satDirectSubtransX-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.satDirectSubtransX-datatype

**Path:** `cim:SynchronousMachine.satDirectSubtransX`  
**Name:** SynchronousMachine.satDirectSubtransX-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.satDirectSyncX-cardinality

**Path:** `cim:SynchronousMachine.satDirectSyncX`  
**Name:** SynchronousMachine.satDirectSyncX-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.satDirectSyncX-datatype

**Path:** `cim:SynchronousMachine.satDirectSyncX`  
**Name:** SynchronousMachine.satDirectSyncX-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.satDirectTransX-cardinality

**Path:** `cim:SynchronousMachine.satDirectTransX`  
**Name:** SynchronousMachine.satDirectTransX-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.satDirectTransX-datatype

**Path:** `cim:SynchronousMachine.satDirectTransX`  
**Name:** SynchronousMachine.satDirectTransX-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.shortCircuitRotorType-cardinality

**Path:** `cim:SynchronousMachine.shortCircuitRotorType`  
**Name:** SynchronousMachine.shortCircuitRotorType-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.shortCircuitRotorType-datatype

**Path:** `cim:SynchronousMachine.shortCircuitRotorType`  
**Name:** SynchronousMachine.shortCircuitRotorType-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not IRI (Internationalized Resource Identifier) or it is enumerated value not part of the profile."

**Constraints:**

- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:IRI` 
- **sh:InConstraintComponent** (Severity: sh:Violation)
  - Values: `[cim:ShortCircuitRotorKind.salientPole1 cim:ShortCircuitRotorKind.turboSeries2 cim:ShortCircuitRotorKind.salientPole2 cim:ShortCircuitRotorKind.turboSeries1]` 

### sc:SynchronousMachine.voltageRegulationRange-datatype

**Path:** `cim:SynchronousMachine.voltageRegulationRange`  
**Name:** SynchronousMachine.voltageRegulationRange-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.voltageRegulationRange-cardinality

**Path:** `cim:SynchronousMachine.voltageRegulationRange`  
**Name:** SynchronousMachine.voltageRegulationRange-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Cardinality violation. Upper bound shall be 1"

**Constraints:**

- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.x0-datatype

**Path:** `cim:SynchronousMachine.x0`  
**Name:** SynchronousMachine.x0-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

### sc:SynchronousMachine.x0-cardinality

**Path:** `cim:SynchronousMachine.x0`  
**Name:** SynchronousMachine.x0-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.x2-cardinality

**Path:** `cim:SynchronousMachine.x2`  
**Name:** SynchronousMachine.x2-cardinality  
This constraint validates the cardinality of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "Missing required property (attribute)."

**Constraints:**

- **sh:MinCountConstraintComponent** (Severity: sh:Violation)
  - MinCount: `1` 
- **sh:MaxCountConstraintComponent** (Severity: sh:Violation)
  - MaxCount: `1` 

### sc:SynchronousMachine.x2-datatype

**Path:** `cim:SynchronousMachine.x2`  
**Name:** SynchronousMachine.x2-datatype  
This constraint validates the datatype of the property (attribute).

**Severity:** sh:Violation

**Messages:**
- "The datatype is not literal or it violates the xsd datatype."

**Constraints:**

- **sh:DatatypeConstraintComponent** (Severity: sh:Violation)
  - Datatype: `xsd:float` 
- **sh:NodeKindConstraintComponent** (Severity: sh:Violation)
  - NodeKind: `sh:Literal` 

